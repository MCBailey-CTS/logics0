from Loc import Loc
from _puzzles import Magnets, Mathrax

PLUS = 1
MINUS = 0
EMPTY = MINUS

class MathraxCrossHatch:

    def solve0(self, puzzle: Mathrax) -> int:
        edits = 0
        return edits
